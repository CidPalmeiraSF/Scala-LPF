def sub(l:List[Int],m:Int,n:Int):List[Int] ={
  if(m>0) sub(l.tail,m-1,n-1)
  else
    if(n>=0) l.head::sub(l.tail,m,n-1)
    else Nil
}

val l1 = List(0,1,2,3,4,5,6,7,8)

val l2 = List(2,5,4,3,5,7,9)

println(sub(l1,2,5))
println(sub(l2,1,3))
